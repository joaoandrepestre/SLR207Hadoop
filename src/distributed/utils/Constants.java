package distributed.utils;

/* 
* The Constants class provides a set of constants used by the whole system
*/
public interface Constants {

    public static final int TIMEOUT = 15; /* timeout for external processes */
    public static final String USER = ""; /* user name  */
    public static final String BASEDIR = "/tmp/" + USER; /* base directory used by the system to store data */

}